name: codex-review-gate
run-name: codex-review-gate pr-${{ github.event.pull_request.number || github.event.issue.number || github.event.client_payload.pr_number }}

on:
  pull_request_target:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review, converted_to_draft, edited]
  issue_comment:
    types: [created, edited, deleted]
  repository_dispatch:
    types: [codex-review-reconcile]
  push:
    branches: [main]

permissions: {}

concurrency:
  group: codex-review-gate-${{ github.event.pull_request.number || github.event.issue.number || github.event.client_payload.pr_number || github.ref }}
  cancel-in-progress: false

jobs:
  reconcile:
    name: refresh-codex-review-status
    if: ${{ github.event_name == 'pull_request_target' || github.event_name == 'repository_dispatch' || (github.event_name == 'issue_comment' && github.event.issue.pull_request != null) }}
    permissions:
      contents: read
      issues: read
      pull-requests: read
      statuses: write
    runs-on: ubuntu-latest
    steps:
      - name: Check out the trusted default branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Reconcile Codex evidence against the live PR head
        env:
          GITHUB_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number || github.event.issue.number || github.event.client_payload.pr_number }}
          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || '' }}
          GITHUB_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          if [[ -z "${PR_NUMBER}" ]]; then
            echo "A concrete pull request number is required." >&2
            exit 2
          fi
          python3 -I -B scripts/check_codex_review.py --publish

  dispatch-open-pull-requests:
    name: reconcile-open-main-pull-requests
    if: github.event_name == 'push'
    permissions:
      contents: write
      pull-requests: read
    runs-on: ubuntu-latest
    steps:
      - name: Reconcile every open PR against the new trusted base
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh api --paginate \
            "repos/${GITHUB_REPOSITORY}/pulls?state=open&base=main&per_page=100" \
            --jq '.[].number' |
          while IFS= read -r pull_request_number; do
            [[ -n "${pull_request_number}" ]] || continue
            gh api --method POST \
              "repos/${GITHUB_REPOSITORY}/dispatches" \
              -f event_type=codex-review-reconcile \
              -F "client_payload[pr_number]=${pull_request_number}"
          done
