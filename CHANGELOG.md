# Changelog

## Unreleased

- Initial project structure
- Mihomo TUN + rules + LazyCat dashboard app
- Dashboard switched to metacubexd (auto-fetch latest release on deploy)
- Systemd deploy script now installs mihomo + Country.mmdb and auto-generates controller secret
- Added docker compose deployment option + mihomo-manager + selfcheck
