This directory is the LazyCat LPK `contentdir`.

The actual dashboard UI is the Clash Verge Rev web port and is generated by `pnpm build`.

Common workflows:

- Sync upstream vendor source:
  - `scripts/sync_clash_verge_rev.sh`

- Build + install the LazyCat dashboard app:
  - `scripts/deploy_dashboard.sh`
